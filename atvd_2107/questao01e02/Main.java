/*
QUESTAO 01 - RESPOSTA:

Classe abstrata e interface servem pra coisas parecidas, mas com um objetivo diferente. 
A classe abstrata serve pra representar classes que são do mesmo tipo, tipo uma base 
comum entre elas. Já a interface serve mais pra garantir que uma classe tenha certo 
comportamento, mesmo que ela não tenha nada a ver com outra. Nenhuma das duas pode ser
instanciada direto, ou seja, não dá pra criar um objeto delas com new, as duas só 
servem de base pra outras classes. Nos métodos, a classe abstrata pode ter método já 
pronto e também método abstrato, que a subclasse é obrigada a implementar. A interface
antes só podia ter método sem corpo, mas hoje em dia, desde o Java 8, ela também pode
ter método default, que já vem pronto. Nos atributos, a classe abstrata pode ter 
atributo normal e construtor. A interface não, ela só pode ter constante. E na herança, 
uma classe só pode herdar de uma classe abstrata, mas pode implementar várias interfaces
ao mesmo tempo.

QUESTAO 02 - RESPOSTA: 

(V) Uma classe abstrata pode possuir métodos concretos.
(V) Uma interface pode possuir métodos default.
(F) Uma classe pode herdar de várias classes abstratas.
(V) Uma classe pode implementar várias interfaces.
(V) Uma interface pode possuir constantes.

*/










